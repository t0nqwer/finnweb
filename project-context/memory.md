- Working pricing baseline in this codebase is: Free / Basic / Business / Pro.
- Treat pricing tiers above as the default for implementation and documentation unless a new decision overrides it.
- In user-facing pricing UI, do not introduce alternate tier names (for example `Ultimate`) for existing plan codes; display canonical names only.
- Rate limiting for auth endpoints uses Redis-backed counters (guards in `/common/guards/rate-limit.guard.ts`) wrapped by convenient decorators in `/common/decorators/rate-limit.decorator.ts`. Key generator can be IP-based or email-based. Error responses use HttpException with error codes that frontend can translate.
- Stripe billing flow: checkout session (POST /billing/checkout-session) → Stripe hosted checkout → checkout.session.completed webhook → customer.subscription.created/updated webhook → subscription upserted to DB with isCurrent=true, marking previous subscriptions as not current. Frontend queries GET /billing/subscription?workspaceId=X to fetch current plan details for dashboard display.
- Subscription cancellation is reversible: cancel_at_period_end flag on Stripe + cancelAtPeriodEnd boolean in DB; POST /billing/reactivate removes the flag before period ends to allow user to undo; GET /billing/subscription exposes both cancelAtPeriodEnd and latestPaymentStatus for UI to show recovery CTA and payment failure state. Payment failures (invoice.payment_failed webhook) create FAILED payment record and set subscription to PAST_DUE status; on subscription deletion, system automatically creates FREE subscription fallback (handleSubscriptionDeleted).
- Brand system source of truth: `project-context/brand-book.md`.
- Frontend default theme: Deep Space dark mode (`#1A1C23`) unless explicitly changed by product decision.
- Thai-centric UI copy and typography rules are required for customer-facing surfaces (Kanit; line-height >= 1.7 for Thai-heavy text blocks).
- Canonical pricing matrix was updated on 2026-04-18 and is now the official baseline for package copy/features: Free (ทดลองเล่น/นามบัตร), Basic (เริ่มต้นมีหน้าร้าน), Business (สายขาย/ยิง Ads - แนะนำ), Pro (เอเจนซี่/บริษัทใหญ่). Source: `project-context/pricing-feature-matrix.md`.
- Plan-level highlight from 2026-04-18 matrix: Free/BASIC/BUSINESS/PRO website limits = 1/1/3/10 and page limits = 1/3/10/50; LINE OA quotas = 5/mo, 50/mo, unlimited, unlimited; support tiers escalate from Help Center to Exclusive Advisor.
- Frontend plan metadata source of truth lives in `apps/web/src/lib/plan-catalog.ts`; pages under `/pricing`, `/subscription`, and `/subscription/checkout` must consume this module to prevent copy/price/feature drift.
- Canonical plan capability fields now exist in Prisma `Plan`: `lineOaMonthlyQuota` (null = unlimited), `supportTier`, `trackingLevel`, `analyticsLevel`; `/billing/plan-usage` and `/billing/subscription` must expose these fields for frontend gating and package UI.
- Product terminology baseline changed: use `LINE OA` everywhere (UI/API/docs/schema) and avoid introducing `LINE Notify` references in new work.
- LINE OA quota enforcement uses current-month `FormSubmission` counts for forms with `lineOaAccessToken` configured. Public lead submissions for LINE-enabled forms now hard-block with `LINE_OA_QUOTA_REACHED` when the current plan quota is exhausted; `lineOaMonthlyQuota = null` remains unlimited for Business/Pro.
- Public lead capture flow now exists at `POST /api/public/sites/:siteId/forms/submit` (Sites module). Backend upserts a deterministic public `Form` (`public-{sectionId|pageId|site}`) and writes `FormSubmission` with normalized contact data plus `ipAddress`, `userAgent`, `referrer`, and UTM values parsed from referrer querystring.
- Website template system V1 is live with API-first flow: `GET /api/templates?scope=official|my|all`, `GET /api/templates/:id`, `POST /api/templates`, `PATCH /api/templates/:id`, `POST /api/templates/:id/apply`, and `POST /api/sites` now supports `templateId` for one-shot site bootstrap.
- Template library baseline now includes 5 official multi-page templates seeded from `apps/api/prisma/seed.ts`; each template stores page/section structure in `TemplatePage` + `TemplateSection` and has active snapshot version in `TemplateVersion`.
- New section types supported for template-driven websites: `NAVBAR`, `SIDEBAR`, `HEADER`, `FOOTER`, `BOOKING`, `COMPARISON`, `CONTENT` (in addition to existing section types). Validation rules now explicitly cover menu/cta, booking fields, comparison lists, and sourceMode/itemLimit for `NEWS_LIST` and `PRODUCT_GRID`.
- Frontend create-site flow (`/sites/create`) now consumes template data from API and creates site by passing `templateId`; avoid reintroducing client-side manual section seeding in this flow unless backend install path is unavailable.
- `/sites/create` follows the site-builder brief flow: answer business questions first, choose an API template second, then create via `POST /api/sites` with `templateId`. Business answers now include `businessType`, `goal`, `style`, `language`, phone, LINE ID, and logo URL for backend template placeholder replacement.
- Create-site frontend code is now feature-scoped under `apps/web/src/features/site-create/`; keep `apps/web/src/app/sites/create/page.tsx` as a tiny route wrapper that renders `CreateSiteWizard`.
- After successful create-site API response, `CreateSiteWizard` must redirect to `/sites/{siteId}/builder`; do not render `SiteEditorSimulator` from the create flow.
- First real builder shell UI lives under `apps/web/src/features/builder/components/` and `/sites/[siteId]/builder` renders `BuilderShell` with local mock sections only. Keep API/drag-and-drop out until the builder data wiring task.
- Builder canvas now renders mock sections through `features/builder/registry/section-registry.ts` using string keys (`hero.splitImage`, `features.grid`, `contact.lineCta`, `footer.simple`) and a safe fallback for unknown section types.
- Builder edit panel is schema-driven from each registry entry's `editorSchema`; supported local field types are text, textarea, url, image, color, select, and switch. Keep edits local-only until autosave/API wiring is explicitly added.
- `/sites/{siteId}/builder` now loads real pages and sections via authenticated APIs (`GET /sites/:siteId/pages`, `GET /sites/:siteId/pages/:pageId/sections`) and adapts backend section types to registry render keys for local preview editing.
- Builder section prop edits now autosave with optimistic local state and an 800ms debounce through `PATCH /sites/:siteId/pages/:pageId/sections/:sectionId`; stale save responses are ignored so latest edits win.
- Builder selection is synced across left section list, canvas, and right edit panel. Left panel actions include real API-backed add, duplicate, delete, hide/show via `isVisible`, and reorder with optimistic rollback on API failure.
- Builder section reorder uses simple up/down controls in the left panel and posts ordered section IDs to `POST /sites/:siteId/pages/:pageId/sections/reorder`; local order updates optimistically and rolls back on API failure.
- Template matching metadata lives in `Template.tags` JSON for now, exposed by the templates API as top-level `businessTypes`, `goals`, `styles`, `languages`, and `keywords`; frontend create-site matching should use these explicit fields first and only fall back to slug/category inference for older templates.
- Site editor now supports `Save as template` (owner-scoped custom template creation) by collecting current pages + sections and calling `POST /api/templates`.
- Section Template Library V1.1 baseline is now introduced: section-level templates are separate entities (`SectionTemplate` + `SectionTemplateVersion`) and sections can be switched with same-type canonical mapping while preserving unmatched legacy data in fallback JSON.
- Section template APIs are available at `GET /api/section-templates`, `GET /api/section-templates/:id`, and section switch endpoint `PATCH /api/sites/:siteId/pages/:pageId/sections/:sectionId/template`.
- Site editor add-section flow now goes through template picker and section settings support template switching; renderer remains structured (no raw HTML injection).
- NAVBAR section templates now have 10 official responsive layouts (desktop + mobile) and are selectable from section template picker; preview renderer chooses layout by section template code.
- SectionTemplateVersion now supports DB-driven render payload (`renderMode`, `htmlTemplate`, `cssTemplate`) and editor preview prioritizes SAFE_HTML templates from DB with structured-render fallback for compatibility.
- For SAFE_HTML section templates, avoid pushing template-specific CSS into `apps/web/src/app/globals.css`; use runtime style registry in editor/public renderers to inject only CSS for templates present on the current page, deduped by style key/hash, and pass theme values via CSS variables (for scale to thousands of templates).
- Rework baseline (2026-05-01): follow site-builder brief direction by treating raw HTML section templates as non-MVP rendering path in builder preview; prefer structured section rendering from section props/type for stability and maintainability.
- Builder rework (2026-05-01): section registry and editor schema should live outside simulator/component files under `features/builder/*` so renderer/editor behavior can be reused by future builder routes.
- Public render path must resolve content from latest `PublishLog.snapshot` only (not draft `Page/Section` rows), and publish action should create a complete site/page/section snapshot for deterministic rendering.
